# Secrets
.env

# Data & databases
data/*.db
data/*.sqlite
data/raw/
*.csv
# ...except the project's data store — the nightly refresh Action reads and
# updates it in place, so it lives in the repo (a few MB).
!data/solesight.db

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.ipynb_checkpoints/

# HuggingFace / model caches
.cache/
~/.cache/huggingface/

# Prophet / cmdstan artifacts
*.stan
*.hpp

# OS / editor
.DS_Store
.idea/
.vscode/
.claude/settings.local.json
